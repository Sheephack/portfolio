# Portfolio proyect created with React.

Under first stages of development, this project is a simple portfolio using Typescript, React, and Styled Components.

## Changelog (in order to keep track of changes):

### v0.3.0:
- About me section (`InfoSection.tsx`) updated with Parallax.js.
    - Parallax.js is a library that allows to create a parallax effect on the background image.
    - Added declaration of `parallax-js` to `parallaxDeclaration.d.ts`.
    - Created assets for the parallax effect (3 building and 1 cloud asset).
- Added `PortfolioSection.tsx` component.
- Added `Divisior.tsx` component.
- Updated `Sidebar.tsx` component to reflect changes on other sections.
### v0.2.0:
- Added a simple about me section (not finished).
- Fixed the use of internal links (proper use of react-router-dom).
- Fixed the use of interface and props (to accommodate to TS use).
- Added profile picture.
- Added several styles to the project.
- Sidebar and navigation bar are now styled properly.
- Added tooltips to the navbar.
- Added distortion effect to personal name/branding.
### v0.1.0: 
- Development started. 
- Dark mode and theme support added.
- Basic layout (not finished).